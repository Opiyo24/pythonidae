"""Finds the biggest integer of a list
    Loops through all list items, if item is larger than previous, store new iotem
            if not larger than previous maintain previous item
            
    If list is empty return none
    Assume lit contains integers only"""